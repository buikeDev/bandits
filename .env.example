# Database
DATABASE_URL=postgresql://user:password@localhost:5432/bandit
DIRECT_URL=postgresql://user:password@localhost:5432/bandit

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:3001/api
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Backend
PORT=3001
NODE_ENV=development
CORS_ORIGIN=http://localhost:3000

# Customer social sign-in (backend only)
SUPABASE_URL=https://vphjyzadnkdlmfauvpwk.supabase.co
SUPABASE_PUBLISHABLE_KEY=
CUSTOMER_AUTH_ORIGIN=http://localhost:3000
CUSTOMER_GOOGLE_ENABLED=false
CUSTOMER_APPLE_ENABLED=false

# Newsletter (backend only; never NEXT_PUBLIC_)
MAILCHIMP_API_KEY=
MAILCHIMP_SERVER_PREFIX=
MAILCHIMP_AUDIENCE_ID=

# Enable staff links only after the admin migration and dashboard are deployed.
# ADMIN_DASHBOARD_URL=https://band-it-frontend.onrender.com

# Backend abuse protection; see docs/ABUSE_PROTECTION.md
ABUSE_STORE=memory
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
ABUSE_KEY_SECRET=
TRUSTED_PROXY_CIDRS=

# Transactional email; migrate database before deployment.
ORDER_NOTIFICATIONS_ENABLED=false
RESEND_API_KEY=
ORDER_EMAIL_FROM=

# Private customer artwork uploaded through the backend.
ORDER_ARTWORK_BUCKET=order-artwork
