# Render frontend: browser requests stay on the frontend origin.
NEXT_PUBLIC_API_URL=/api
# Server-side upstream origin; no trailing /api.
BACKEND_URL=https://band-it-backend.onrender.com
# Public storefront origin used for canonical URLs, social cards and sitemap URLs.
NEXT_PUBLIC_SITE_URL=https://band-it-frontend.onrender.com
